```python
### zip
lst1 = [1, 2, 3]
lst2 = ['x', 'y', 'z']
'''
参数类型为列表list，参数个数为多个（可大于2），返回值为迭代器
会把列表索引相同的数据放到一个tuple内，若列表长度不同，取最短长度
结果为：[(1, 'x'), (2, 'y'), (3, 'z')]
'''
res = zip(lst1, lst2) # res 返回的是迭代器

# 解压还原， 还原为两个列表
a2,b2 = zip(*z)


### next(iter) 调用迭代器.__next__()
next(lst1)
```