
```javascript
const arr = new Uint16Array(
    buffer, // arrayBuffer
    4, // byteoffset 字节偏移，从第几个字节开始读取
    3  // 读取长度，表示读取几个Uint16，而不是字节
)
// Uint16 16位整数 即2个字节
// arr.length → 3（3个元素）
// arr.byteLength → 6（总共占6字节）

```