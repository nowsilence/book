createImageBitmap 
整个解码过程是在后台线程处理的，返回的是一块GPU可以使用的内存

浏览器对同时进行的图像解码任务有数量限制（不同浏览器实现不同，通常为 4-8 个并发）。超过限制时，新的解码任务会被阻塞或直接失败，因为浏览器无法分配足够的解码线程 / 内存。

createImageBitmap 会占用 GPU 和系统内存，大量并发调用可能导致内存不足，进而触发解码失败。

比较大的图片解码会非常耗时