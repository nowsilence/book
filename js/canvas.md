```javascript

// 画布大小，不一定是显示大小，会缩放到style.width/style.height定位的范围内
canvas.width = 1600;
canvas.height = 800;

// 显示大小，如果未定义，则默认为和canvas.width/canva.height大小一样
canvas.style.width = '800px';
cnavas.style.height = '400px';
```