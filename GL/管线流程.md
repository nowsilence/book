CPU 准备数据 → 
顶点着色器 → （输出的是裁剪空间坐标）
图元装配 & 裁剪 → （裁剪的最小单位是「图元」，不是单个顶点，所以必须先把顶点组装成完整图元，再执行裁剪）
透视除法（转ndc坐标） & 视口变换（ndc转屏幕坐标） → 
光栅化 → 
片元着色器 → 
模板测试 → 
深度测试 → 
颜色混合 → 
帧缓冲区写入

光栅化的过程:
将矢量图元（三角形 / 线 / 点）转换成一个个片元（待渲染像素单元）
对顶点着色器传出的 varying 数据做重心插值，让图元内部属性平滑过渡
生成的所有片元送入片元着色器